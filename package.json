{
	"name": "zifiv",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "pnpm --filter @zifiv/web build",
		"dev": "pnpm --filter @zifiv/web dev",
		"check:fix": "biome check --write",
		"check:fix:unsafe": "biome check --write --unsafe",
		"check": "biome check",
		"start": "next start",
		"prod-deploy": "sst deploy --stage production",
		"prod-remove": "sst remove --stage production",
		"sst:dev": "sst dev --stage production"
	},
	"devDependencies": {
		"@biomejs/biome": "^2",
		"@types/node": "^20",
		"typescript": "^5"
	},
	"dependencies": {
		"sst": "^3.17.23"
	}
}
